const { resolve } = require('path');

module.exports = {
  'config': resolve(__dirname, 'src', 'configs', 'databases.json'),
  'models-path': resolve(__dirname, 'src', 'models'),
  'migrations-path': resolve(__dirname, 'src', 'databases', 'migrations'),
  'seeders-path': resolve(__dirname, 'src', 'databases', 'seeds')
}
